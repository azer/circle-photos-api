#!/usr/bin/env node

require('default-debug')('flickr-favorites-api:server,flickr-favorites-api:favorites,flickr-client');
require('with-env')();

require('../').start(process.argv[2] || 8080, '0.0.0.0');
